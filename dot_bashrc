# .bashrc
#
# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]; then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
export PATH

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions
if [ -d ~/.bashrc.d ]; then
    for rc in ~/.bashrc.d/*; do
        if [ -f "$rc" ]; then
            . "$rc"
        fi
    done
fi
unset rc

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/alex/miniforge3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/alex/miniforge3/etc/profile.d/conda.sh" ]; then
        . "/home/alex/miniforge3/etc/profile.d/conda.sh"
    else
        export PATH="/home/alex/miniforge3/bin:$PATH"
    fi
fi
unset __conda_setup

if [ -f "/home/alex/miniforge3/etc/profile.d/mamba.sh" ]; then
    . "/home/alex/miniforge3/etc/profile.d/mamba.sh"
fi
# <<< conda initialize <<<

HISTSIZE=10000
HISTFILESIZE=100000

source "$HOME/.cargo/env"
source "$HOME/sources/spack/share/spack/setup-env.sh"
export PATH=/home/alex/go/bin:$PATH
eval "$(starship init bash)"
export BAT_THEME="Coldark-Dark"
export GIT_EDITOR=vim
export EDITOR=vim
export TERM=xterm-256color
alias ls=lsd
alias la="lsd -alh"
alias cat=bat
alias pts=phoronix-test-suite
alias update="sudo dnf update"
#fortune | cowsay -f tux
